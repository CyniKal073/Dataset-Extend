# Dataset-Extend
A dataset extension python script

This python script contains image rotating and image enhancement

Ultimate saved dirs (imgs and labels) are `test_label/` and `test_label_enhance/`

Also available for xml format.
